{% comment %}
** Divider - homepage partial **
- Draggable section
{% endcomment %}

{% include 'include-divider' %}

{% schema %}

{
  "name": "Divider",
  "class": "divider-section",
  "settings": [
    {
      "type": "range",
      "id": "divider_spacing",
      "min": 0,
      "max": 30,
      "step": 5,
      "unit": "px",
      "label": "Space",
      "default": 20
    },
    {
      "type": "checkbox",
      "id": "divider_border",
      "label": "Show border",
      "default": false
    }
  ],
  "presets": [
    {
      "name": "Divider",
      "category": "Layout",
      "settings": {

      }
    }
  ]
}

{% endschema %}